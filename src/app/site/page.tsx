import type { Metadata } from "next";
import { APP_NAME } from "@/lib/brand";

export const metadata: Metadata = {
  title: `PDV no celular · ${APP_NAME}`,
  description:
    "Transforme seu celular em uma maquininha de Pix. Sem taxa por venda, funciona offline.",
};

export default function SitePage() {
  return null;
}
